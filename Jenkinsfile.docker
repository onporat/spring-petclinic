pipeline {
    agent any
    stages {
        stage ('Docker build') {
            steps {
                sh '''
                  cp -rv /tmp/target .
                  docker build -f Dockerfile -t spring-petclinic-docker .
                '''
            }
        }
    }
}